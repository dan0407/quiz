const studentGrade = [100, 50, 65, 70, 85, 96, 45];

for (let index = 0; index < studentGrade.length; index++) {
	const element = studentGrade[index];
}
function esSuficientementeGrande(studentGrade) {
	return elemento < 60;
}
var filtrados = [100, 50, 65, 70, 85, 96, 45].filter(studentGrade);

const result = computerGrade(studentGrade);
console.log(result);
